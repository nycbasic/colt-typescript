"use strict";
// const getRandomElement = <T,>(list: T[]): T => {
//     const randIdx = Math.floor(Math.random() * list.length);
//     return list[randIdx];
// } 
// when using tsx files for react, for generic arrow functions, the comma is use to keep the auto-complete as a closing element tag from confusing TS generics.
